# SVC MotM 한글판 v17.2 — 잔여 원본 타일 조사

`tools/tiletool/`로 조작 경로 9화면을 관측하고, 원본 롬 `SNK Vs Capcom - Match of The
Millennium (JUE) [!]`와 대조한 결과다. 조사일 2026-08-24.

판정은 주소가 아니라 **내용**으로 한다. 화면에 뜬 타일의 16바이트가 원본 롬 어디에도
없으면 새로 그린 타일이다. (4MB 롬에서는 같은 타일이 여러 주소에 중복돼 주소 비교가 흔들린다.)

```bash
python3 tools/tiletool/observe.py SVC_kr_v17.2.ngc rec_svc/ screens_svc.json
python3 tools/tiletool/diffmap.py rec_svc/ 원본.ngc SVC_kr_v17.2.ngc \
        --lines 줄/ --min-run 4 --csv 남은것.csv
```

## 결과

관측 9화면에 뜬 고유 ROM 타일 **1,551개 중 새로 그린 것은 41개(2.6%)** 뿐이다.

| 화면 | 뜬 타일 | 새 타일 | 원본 잔여 |
|---|---|---|---|
| 타이틀 | 417 | **0** | 417 |
| 메인메뉴 | 208 | 26 | 182 |
| 게임선택 | 107 | 5 | 102 |
| 스타일선택 | 191 | 15 | 176 |
| 플레이어선택 | 335 | **0** | 335 |
| VS화면 | 208 | **0** | 208 |
| 월드맵 | 226 | **0** | 226 |
| 대전준비 | 290 | **0** | 290 |
| 전투 | 276 | 7 | 269 |
| **합계(고유)** | **1,551** | **41** | **1,510** |

손댄 것은 메뉴 항목 글자뿐이다 — `토너먼트`·`통신`·`올림픽`·`등록`·`스파링`·`현혁`,
`싱글`·`태그`·`팀`, `균형`·`반격`·`속공`, 안내 문구 몇 줄.

참고로 같은 잣대로 잰 SS2 v0.99b는 1,159개 중 **321개가 새 타일(28%)**이다.

## 남은 것 — 화면별 가로줄

`--lines`가 뽑은, **화면에서 가로로 4타일 이상 이어진 원본 잔여 줄** 100개의 분포다.
(같은 타일 반복인 배경 채움과 32칸 통줄은 제외했다.)

| 화면 | 줄 수 | 실제로 남은 것 |
|---|---|---|
| 플레이어선택 | 33 | `PLAYER SELECT` 머리글, 캐릭터 이름판(`KYO` 등), `VS`·`?` 아이콘 |
| VS화면 | 17 | VS 연출 그래픽 |
| 타이틀 | 15 | 로고 「頂上決戦 最強ファイターズ」, 「Aボタンを おし ください」, 저작권 줄 |
| 메인메뉴 | 13 | `MAIN MENU` 머리글, 버튼 테두리 |
| 스타일선택 | 9 | `STYLE SELECT` 머리글, 버튼 테두리 |
| 대전준비 | 6 | 배경 |
| 게임선택 | 4 | `GAME SELECT` 머리글 |
| 전투 | 3 | HUD |

### 우선순위

1. ~~**타이틀 로고와 안내문**~~ — **머리글·안내문은 처리했다** ([`tools/svc/`](../tools/svc/)).
   「頂上決戦」→「정상결전」(타일맵 32타일), 「Aボタンを おし くださ」→「A버튼을 눌러 주세요」(스프라이트 12타일).
   큰 로고 「最強ファイターズ」는 타일 119장짜리 스타일 아트라 남아 있다 — 사람이 그려야 한다
2. ~~**영문 머리글**~~ — `MAIN MENU` 는 처리했다([`tools/svc/build_header_ko.py`](../tools/svc/build_header_ko.py)).
   `GAME SELECT`·`STYLE SELECT` 는 **막혔다** — 아래 「머리글이 막힌 이유」 참고.
   `PLAYER SELECT` 는 스프라이트가 아니라 타일맵이라 별도 조사가 필요하다
3. **캐릭터 이름판** — `KYO` 등. 32명분이라 분량이 가장 크다
4. 저작권 줄·HUD·배경은 번역 대상이 아니다

## 한계

- `--lines`의 "원본 잔여"에는 **그림 타일도 섞인다.** 미번역 글자만 자동으로 걸러내지는 못한다.
  줄 그림을 눈으로 훑어 고르는 단계가 남는다
- 관측 9화면 밖(스파링·올림픽·통신 대전·엔딩 등)은 아직 안 봤다.
  `screens_svc.json`에 경로를 추가하면 그만큼 커버리지가 늘어난다


## 머리글이 막힌 이유 (조사 기록)

`GAME SELECT`·`STYLE SELECT` 를 `MAIN MENU` 와 같은 방식으로 처리하려다 실패했다.
같은 길을 다시 걷지 않도록 알아낸 것과 막힌 지점을 남긴다.

### 구조

세 머리글은 **하나의 스프라이트 타일 풀**(`0x22A800`~`0x22AF00`, 112장)을 나눠 쓴다.
풀의 타일마다 번호를 점으로 심은 롬을 만들어 화면에서 읽어 확정했다.

    MAIN MENU     타일 0~26    (0x22A800~0x22A9A0)
    STYLE SELECT  타일 27~62   (0x22A9B0~0x22ABE0)
    GAME SELECT   타일 64~98   (0x22AC00~0x22AE20)

y 값이 여러 개로 나오는 것은 애니메이션이 아니다. 글자 띠의 **비스듬한 끝동강**이
별도 스프라이트로 붙기 때문이고, 글자 자체는 가로 한 줄이다.
(정지 900프레임까지 지켜봐도 좌표가 안 변한다 — 애니메이션이 아니다.)

### 막힌 지점

**파란 띠 타일이 세 화면에 걸쳐 공유된다.** 한 화면 기준으로 띠를 다시 그려 써 넣으면
다른 두 화면의 띠가 같이 깨진다. 실제로 세 화면을 한 롬에 구웠더니 셋 다 깨졌다.

`MAIN MENU` 가 되는 이유는 **그 화면에만 있는 타일만 건드리는 배치**를 손으로 골랐기 때문이다
(「메」를 x0·x48 에 두어 공유 칸을 빈 칸으로 유지). 나머지 둘도 같은 수작업이 필요하다.

공유 타일을 아예 안 건드리는 방법도 해 봤는데, 한글은 원판 글자가 없던 자리에도 획이
필요해서 글자가 군데군데 빠졌다.

### 헛다리 세 개 (다시 하지 말 것)

1. **주소를 내용으로 찾기** — 띠 타일은 여러 장이 내용이 같아서 첫 일치를 쓰면
   남의 자리에 글자를 써 넣는다. 번호를 심어 화면에서 읽어야 한다
2. **띠를 끝동강 사이로 보간** — 끝동강이 뾰족해서 가운데가 얇아진다
3. **띠 범위를 파란 픽셀로만 재기** — 글자에 가린 열은 파란 픽셀이 없다.
   **원판 글자가 있던 자리도 띠에 포함**시켜야 원래 두께가 나온다

### 다음에 할 일

- 화면별로 **그 화면 전용 타일**이 어디까지인지 목록화하고, 그 안에 들어가는 배치를 짠다
- 아니면 스프라이트 표를 쓰는 코드를 찾아 타일 번호를 바꿔, 화면마다 전용 타일을 주게 한다
